export const imagesBasePath = 'assets/images';
export const config = {
  apiTimeoutMs: 10000,
  convoyLimit: 4,
  image: {
    paths: {
      empty: `${imagesBasePath}/misc/Empty.svg`,
      planets: `${imagesBasePath}/planets`,
      vehicles: `${imagesBasePath}/vehicles`
    }
  }
};
